# Django Core Settings
SECRET_KEY=your-secret-key-here-minimum-50-characters
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# By default, SQLite is used for development
# For production, set DATABASE_URL to use PostgreSQL, MySQL, etc.

# PostgreSQL Example:
# DATABASE_URL=postgresql://username:password@host:5432/database_name

# MySQL Example:
# DATABASE_URL=mysql://username:password@host:3306/database_name

# Heroku Postgres (automatically set by Heroku):
# DATABASE_URL=postgres://user:password@host:5432/dbname

# Email Configuration
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend  # Override default if needed

# PayPal Configuration (if applicable)
# PAYPAL_CLIENT_ID=your-paypal-client-id
# PAYPAL_SECRET=your-paypal-secret
